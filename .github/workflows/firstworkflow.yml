name: first workflow

on:
  push:
    branches:
      - main
jobs:
  first-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message
      - run: echo "Welcome to first workflow"
